function [x] = GussianColumnElimination(A, b)
% GussianColumnElimination
% 列主元消去法的实现

% 输出A, b, detA
disp('A:');
disp(A);
disp('b:');
disp(b);
disp('detA:');
disp(det(A));

[n, n] = size(A);

% 获取增广矩阵
Ab =  [A, b]


x = 0;

end

